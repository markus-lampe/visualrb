app.visualrb.ui.pages.about = ((page) => {
  const template = `
    <div id="page-about">
    
    
    </div>
  `

  page.show = () => {
    const container = document.getElementById('content')

  }

  return page
})({})
